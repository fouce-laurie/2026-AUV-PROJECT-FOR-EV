# GetParams.srv
# Request:
# - `paths`: 可选的点路径数组（例如 "chassis.pid.pos.kp"）；为空表示返回全部配置
string[] paths
---
# Response:
# - `success`: 是否成功
# - `message`: 失败时的说明
# - `config_json`: JSON 字符串，包含所请求的参数（空 paths 返回完整配置）
bool success
string message
string config_json
